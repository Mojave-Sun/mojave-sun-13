import { Job } from "../base";
import { Town } from "../departments";

const TownDoctor: Job = {
  name: "Town Doctor",
  description: "Aid any patients that come in, whether it be for currency or something more.",
  department: Town,
};

export default TownDoctor;
