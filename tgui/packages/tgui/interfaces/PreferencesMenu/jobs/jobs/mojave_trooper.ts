import { Job } from "../base";
import { NCR } from "../departments";

const NCRTrooper: Job = {
  name: "NCR Trooper",
  description: "Follow and execute orders given to you by your superiors. Don't run off and die a horrible death... Or do.",
  department: NCR,
};

export default NCRTrooper;
