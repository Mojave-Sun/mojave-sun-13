import { Job } from "../base";
import { NCR } from "../departments";

const NCRSergeant: Job = {
  name: "NCR Sergeant",
  description: "Keep troopers in line, obey orders, and relay changes in situations to keep command up to date.",
  department: NCR,
};

export default NCRSergeant;
