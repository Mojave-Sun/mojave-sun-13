import { Job } from "../base";
import { Raiders } from "../departments";

const Raider: Job = {
  name: "Raider",
  description: "Follow the Boss' orders and listen to the Enforcer. Do whatever he says with questionable proficiency.",
  department: Raiders,
};

export default Raider;
